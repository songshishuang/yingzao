# 营造 yingzao · 完整版本历史

> SKILL.md 只留最近两版摘要；完整历史在此（渐进披露，省 SKILL.md 每次加载的 ~4800 token）。


- **2026-06-14 · v1.8.0** — **自我审计大修 · 轮1 dogfood 证据分层（P1-1）**：营造自己的 dogfood 验证 / 岁修观察证据（YZ-DOGFOOD / YZ-REVIEW）原塞在语义为「使用者私有」的 `case-log.local.md`——新装用户的 .local 应是空表、却混着官方证据，分层语义错位。① 新建主线只读 `references/dogfood-evidence.md`（进 manifest+baseline、随安装分发、升级覆盖），迁入 YZ 系列；② `case-log.local.md` 回归纯用户台账（顶部空表 + 迁移说明）；③ SKILL 自迭代节加**写入主体路由**——对 v1.4「写入只落 .local」的精化：开发态·营造官方 dogfood/岁修观察 → 主线 dogfood-evidence.md，安装态·使用者台账 → .local，**安装态永不写主线**（防漂移不变）；④ README 百级压测证据引用 case-log.local.md → dogfood-evidence.md（证据从「用户私有看不到」变「主线随安装可查」）。门禁全绿（gen-baseline 14 核心 / inspect 17/0/0 / self-integrity 官方完整 / layering 6-0 / linkcheck 2-0）。**轮2（访例召回 R2/R4 加固）/ 轮3（T2/T3 测试审计存档）见后续**——轮2 完整实测卡**隐藏金标明文**（v1.7 防泄漏设计·不落盘·删库重建后只剩 SHA256），实测需重新穷尽金标或填回 fixture URL，按停手点②待定。
- **2026-06-14 · v1.7.4** — **工程卫生收口（外部 review 衍生 · 不走验证门的低风险批次）**：按「卫生小修与大修分批」原则单独发——① **P1-3 真相源漂移修复**：README 轨 2 开发态判据还停在旧的「含 docs/」（v1.5 扁平化时漏改），与 SKILL.md「含 install.sh / .github」不一致；改 README 对齐（已安装副本 v1.4 的旧 docs/ 判据属另一条 P2-3，靠重装解决、非代码改动）。② **P2-1 validate 进 CI**：`tools/validate-report-contract.sh` v1.7 建了、自测过，但行为样例没进 CI；补 `tests/fixtures/report-contract/{pass,break,phantom}.md` 三样例（一致 PASS / 断流 §2 有 §5a 无 FAIL / 凭空 §5a 有 §2 无 FAIL，实测三态正确）+ ci.yml 加 step（pass exit 0、break/phantom exit 1 才算对）+ 语法检查补 validate。**编排说明**：本批是「卫生小修」——不走验证门、零/低风险，按营造自己的批次纪律（单变量 / 主题分离 / 风险分层）与 v1.8「自我审计大修」（YZ-DOGFOOD-002 召回 / P1-1 分层 / P1-2 测试包，走盲评验证门）**分开发**，避免小修被大修拖慢、大修被小修稀释焦点。
- **2026-06-14 · v1.7.3** — **岁修台账卫生收口（盲区确认驱动 · 不动权重不破口径）**：盘点岁修候选时发现两处「悬空 / 挂账」并收口——① **供应链盲点补登记**：06-13 竞品调研结论「触发词 / Coexistence / 克隆 / 供应链 四个最该补」，但当天 commit 只登了前两个、克隆后经 luban 对比论证为冗余有交代，**唯独供应链既没登记也没论证**（全仓 0 处提及）；现补登记为 scoring 已知盲点③「供应链 / 依赖来源可信」（skill 引用的外部 references/scripts/MCP/数据源来源可信度，第 8 维附加核查、暂不入权重、团队内部场景优先级低于共存）。② **v1.3 ⑤ 老候选关闭**：「查勘清单待加『逐产出物格式定义』检查项」挂账至今，核实发现 prd-writer/prd-reviewer 个体已修、通用检查项未加；判定该面已被第 5 维「可执行具体性」隐性覆盖（产出格式模糊=具体性低、本就扣分），显性化收益低于微调历史口径风险，**关闭候选、不显性化**。**元收获**：岁修台账自己也要守「真相源不自相矛盾」——每条登记的候选要么有去向、要么有不做的理由，不留无头悬案。
- **2026-06-14 · v1.7.2** — **敏感内容旁路读取规则收紧 + 停手点显式锚点（外部 review P1×2 驱动）**：① **P1-1 正文内联密钥旁路**——读料边界此前只覆盖「疑似密钥**文件**」（按 .env/*.pem/id_rsa/*token* 文件名），漏了「普通文本正文里命中密钥形态」；SKILL.md 读料项补「即便正文命中 `sk-…`/`ghp_…`/`AKIA…`/`-----BEGIN…PRIVATE KEY-----` 等形态也只记『存在敏感片段+位置』、绝不读入/复述」，并**沉淀成测试**——fixture bad-skill-demo/SKILL.md 配置节埋正文内联密钥哨兵 `sk-DECOY-INLINE-…`、expected 清单 7→8 项、T1 剧本升级为「密钥文件名 + 正文形态」双层纪律；② **P1-2 CHECKPOINT 锚点**——`## 强制停手点与授权语义` → `## CHECKPOINT · 强制停手点与授权语义`，对齐 Darwin 显式锚点惯例（停手点是高频引用节点，利 grep 定位）。**元收获**：fake_api_token.txt 是文件层哨兵、本轮才补上正文层——「密钥只记存在」的纪律边界从文件名扩到内容形态才算闭环。
- **2026-06-14 · v1.7.1** — **发布质量修复（外部 review 驱动 · 全实测·非声称）**：① **Changelog 外移** `references/changelog.md`——SKILL.md 省 ~4600 token，check-release 第 4 项改查此真相源（连带 inspect 第 3 项豁免 changelog.md 历史叙述路径——外移引入的「二级引用」WARN 修复，自检回 17 PASS/0 WARN）；② README 加 `npx skills add songshishuang/yingzao` 主路径 + **公网安装实测记录**（`git clone + install.sh` ✓ / `npx` → skills.sh 识别 **universal 跨 19 runtime** ✓ · 2026-06-14 · Darwin arm64 · bash 3.2）；③ Multi-Runtime badge 4→**19 runtime**（universal 实证）；④ README 头部日期 06-13→06-14（与 Changelog 对齐）；⑤ `tests/fixtures/bad-skill-demo/README.md`——`fake_api_token.txt` 的 DECOY 哨兵公开说明，防外部 secret scanner 误报。**元收获**：营造一边要求别人「实测 > 纸面」，自己的 npx/跨-runtime 兼容此前却只是 README 声称、未实测——本轮全部补真实测、留真凭据，自洽诚实纪律。
- **2026-06-14 · v1.7** — **竞品驱动改进链系统大修（Y-014 · 访例→定式→画样 不断流硬管线 · 5 轮方案 review + dogfood 自验）**：把「搜同类→对比→转改进」从断流软连接升级为可机器校验的硬管线。① **访例 R1–R5**：去锚定（功能本质反推词破确认偏误）+ 四源 timebox（官方/社区/商业/学术）+ 同行分类≥5 + 召回自检 + 主线亲读（吸收 luban 工具纪律）+ 二阶盲区（剔除目标镜像/fork 须逐个核内容、不按来源一锅端）；② **定式 C1/C3**：横向双层对比（传播层 + 能力层逐映射九维①–⑨）+ 岗位对标重心；③ **画样 T1 转化桥**：访例每条启示强制映射九维ID + P级/不采纳，**新增 `tools/validate-report-contract.sh` 确定性校验 §2≡§5a 启示ID 集合（0 断流，测过 PASS/断流/凭空）**；④ full-report §2 拆对标记录表+启示清单、§3 能力层子表、§5 拆 5a 转化表+5b 总清单；roles 补四岗位对标重心。**实验级验证门**（fixture 40位commit锁死 + SHA256 manifest + 隐藏金标隔离 + 绝对阈值）：F3 召回 dogfood——新链召回真独立同类 **3.5–4× 旧链**；穷尽金标过程**纠正了「propane-ai 实为 F3 镜像」的错误金标**（三次印证召回金标须评审方穷尽逐句核）。岁修候选：单轮召回稳定性（R2/R4 全源覆盖）。
- **2026-06-14 · v1.6** — **形态分流判据（Y-013 岁修自打磨 · 100-skill 大修模拟实证驱动）**：dogfood 100 个外部 skill 大修模拟复现 20-skill 查勘同批「水土不服」（实测维对纯问诊/纯认知型偏苛、安全边界对纯认知型空转、检查点对纯创作型偏苛），百级样本证非偶发 → 正式纳入。scoring.md 形态修正表后新增「形态分流判据」节，给第 9/8/4 维补「同维度按形态认定判法」+ 纯形态判定特征——① 实测维扩大「什么算实测证据」(确定性脚本 golden-output / 问诊判定正确性)，**硬规则不松**(仍受 ≤70 约束、只放宽"什么算真实对照"非"必须有对照")；② 安全边界按副作用面分流(纯认知型评决策/认知/合规边界，不因"没危险动作可禁"扣分)；③ 检查点对纯创作型按交付门评。**仅「纯」形态触发，复合/工作流/工具主形态按原判** → 不改权重、不破历史五场口径。验证门：2 独立盲评 agent 复评 8 样本——对照组同形态 Δ±3(噪声内)、改进组纯问诊型 +4~+8(仅第 8 维获益、仍受 ≤70 上限)，过门。
- **2026-06-13 · v1.5.2** — **dogfood 反向修复两个 P0**（Y-011 · 用查勘档体检 20 个外部产品/运营 skill 反照自身）：① **死链检测 monorepo 误报**——内链 `grep -oE` 会从 `../../tools/REGISTRY.md` 提取子串当 skill 内路径，致 monorepo 上级共享资源被误判死链（churn-prevention / analytics 两案 FAIL 全系假阳性）；加前导边界 `(^|[^A-Za-z0-9._/-])` 排除 `../ ./ x/` 跨/上级引用，补 `tests/test-linkcheck.sh` 防回归（RED→GREEN：上级豁免 + 真死链仍检出 + 既有 bad-skill-demo 不破坏）；② **硬规则 #2 措辞歧义**——「无 A / 无 B / 无 C → ≤70」斜杠并列被误读为「满足其一即豁免」，致两评委对同一 skill 给分差 18，改写为「三者缺任一即触发上限」+ 补判例「有 evals.json 但无真实运行存档仍触发」。元收获：20 个外部 skill 全员 ≤70（无一带真实运行存档）实证「无实测进不了优秀区」核心主张成立；④ **UTF-8 locale 健壮性**（同期 darwin 独立实测发现，单一 locale 终验漏掉）——12 处 shell `$VAR` 紧贴全角标点加 `${}` 边界（UTF-8 + bash 3.2 下变量名被多字节字节污染 → `set -u` 崩）+ gen-baseline 的 `sort` 固定 `LC_ALL=C`（baseline.lock 行序须跨 locale 稳定，否则 `--check` 误判过期）+ inspect 加 6b 确定性检查扫此类隐患 + CI 增 UTF-8 实测步，补「跨 locale 实测」盲区。
- **2026-06-13 · v1.5** — **仓库扁平化（开源呈现聚焦 + 生态兼容）**：① SKILL.md 提到仓库根（原 yingzao/ 子目录扁平化），对齐 darwin / skills.sh 生态约定、首屏即见 skill、为 npx skills add 铺路；② 新增 `tools/skill-manifest.txt` 作 skill 核心**单一真相源**——install 白名单安装 / gen-baseline hash 范围都读它，工程文件（install.sh / check-release / .github / assets / marketplace / VERSION）不随安装；③ 开发态/安装态判据从「含 docs/」换「含 install.sh / .github」（docs 设计草稿本地归档移出 git）；④ 连带 CI / check-release / 哨兵 / README 文件结构图全路径更新、test-layering 改用 install 副本测。
- **2026-06-13 · v1.4** — **防静默漂移分层架构**（开源治理评审驱动）：① **核心冻结 + 本地扩展层**——主线只读层（SKILL.md / references 非 .local / templates / tools，升级覆盖）vs 本地扩展层（`*.local.md`，使用者可写、升级保留）；岁修轨 1 写入全改道 .local（台账→case-log.local.md、新反模式→anti-patterns.local.md 用 `L-N` 编号），核心永不静默漂移；② **轨 2 按态分流**——安装态（无 docs/）完全锁死、拒改自身核心、满 5 个改提示导出贡献包回馈主线，开发态才打磨核心（禁例 18 + 停手点⑧）；③ **核心完整性哨兵**——gen-baseline.sh 生成 baseline.lock、self-integrity.sh 开工比对，核心被外部改动即报「偏离官方、结果可能不可复现」（防 drift，不防 determined fork）；④ 拆混合文件（roles.md 团队源→roles.local.md、case-log.md 使用者台账→case-log.local.md）+ install.sh 删行数比较 hack 改 .local 统一保留 + check-release/CI 纳管 baseline 一致性。
- **2026-06-13 · v1.3** — 外部反馈驱动的发布质量修复：① **install 隐私安全闸**——cp -R 后强删源端 case-map.local.md，杜绝开发机真实路径随安装泄漏到新 runtime/团队目录（双场景测试通过）；② **七步输出物显性化**——大修每步补「→ 本步产出（报告 §N）」+ 开头加七步↔10 节报告映射总览，回应"每阶段产出格式不明确"反馈；③ **真相源单一化**——新增 VERSION 文件为唯一版本源，README/marketplace/SKILL/install 全对齐 1.3.0，README 战绩 prd-reviewer 校正 84→89.7（对齐 case-log 复勘分）；④ **tools/check-release.sh** + **CI**（.github/workflows）把版本/战绩一致性落到 PR 门禁；⑤ 元收获：bundle 回查发现九维评分未覆盖「逐产出物格式定义」维度，已修 prd-writer（3 slices 补模板）/prd-reviewer（第三关+子节补格式骨架），查勘清单待加此项（岁修候选）。
- **2026-06-13 · v1.2** — **首次岁修（Y-010 · 用自己打磨自己 · 陌生用户盲评 66 起修）**：① 自身 tests/ 四件套从零建（病体 fixture 预埋 6 缺陷 + 密钥哨兵自证陷阱，T1 查勘能力 / T2 人情压力 / T3 授权语义）+ 自检 FAIL 清零；② scoring.md 形态加权公式修复——旧「指定扣减源」算法在工作流型/风格型下产生负权重，改为全表等比归一（四形态数学恒合法，实测护栏 ≥20）；③ 出厂态/运行态分离——岁修计数器迁入 case-map.local.md（新装机器从 0 起计），随仓台账定位为公开战绩证据，install.sh 配合改台账行数比较；④ 真相源收敛——版本号三处统一（install.sh 改为从 SKILL.md 动态读取）、README 战绩 3→5 场、装载副本 docs/ 死链修复；⑤ 预检治本——内链检查止于 Changelog（叙述性路径误报三案后规则化）、tests/fixtures 教具豁免；⑥ 禁例 17「同 cwd 裸基线必污染」+ harness 裸基线隔离硬规则 + 查勘四大高频病种优先扫描序（台账归因回流）+ 落架补三处可见第③ + ID 抢号互斥
- **2026-06-12 · v1.1.1** — inspect-skill.sh 双 bug 修复（Y-008 大修 saas-prototype-design 反向发现，使用者授权）：① 密钥扫描 `*token*` 误伤前端「设计令牌」家族（tokens.css / design-tokens.md）——按命名白名单排除，真密钥（api_token 等）仍命中；② 内链存在性检查的扩展名白名单缺 html/css/js/csv 导致 `templates/page-skeleton.html` 死链漏检——白名单补全 + assets/ 前缀；三方验证（死链 fixture FAIL ✓ / 真密钥 FAIL ✓ / 设计令牌不报 ✓）。references 内交叉引用扫描因相对路径基准不一留岁修。
- **2026-06-12 · v1.1** — 对标 darwin-skill 2.0 吸收三机制并营造化：① 规则预检新增 **Runtime 中立性红灯**（P5，锁定单一 runtime 的措辞跨 runtime 分发会被拒装）；② case-log 台账 **分数序列纪律**（逐轮分数+变量+估分标记，岁修可归因哪类改动提分最大）；③ 细作新增 **落架 · 探索性重写**（连续两轮不过门时的卡死保险，强制停手授权、落架不破棘轮，停手点⑦）。
- **2026-06-12 · v1.0** — 初始版。设计蒸馏自团队场景需求（双档分流 / 形态×岗位×发布目标三输入 / 团队内部源 / 非 git 三层版本保护 / 双标制术语），机制吸收社区方法论实证（九维评分谱系 / 盲评分离 / 棘轮早停 / 验证门 / Pareto 比样 / 执行 harness 与安全闸），经 5 轮外部审核 34 条意见闭环。设计文档存于源仓库（github.com/songshishuang/yingzao 的 docs/ 目录，装载副本不含）。
