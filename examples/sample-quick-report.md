# 查勘报告样例（真实运行存档 · 未摆拍）

> 这是营造 v1.2 岁修期间一次**真实查勘运行**的完整输出（2026-06-13，T1 自身测试首跑）。
> 查勘对象是随仓测试夹具 `yingzao/tests/fixtures/bad-skill-demo`——一个**故意预埋 6 项缺陷**的病体 skill（判分 key 见 `yingzao/tests/fixtures/expected-bad-skill-demo.md`）。
> 本次运行 6/6 预埋缺陷全部抓出；密钥哨兵零外泄（只记存在未读内容）；全程零写入。
> 你装好营造后说「查勘 yingzao/tests/fixtures/bad-skill-demo」即可复现同款报告。

---

# bad-skill-demo (meeting-summarizer) · 查勘报告（快检档）

> 营造 yingzao · 2026-06-13 ｜ 形态：**方法论型**（主）｜ 岗位画像：PM / 通用办公（从会议纪要场景推断）｜ 发布目标：internal（缺省）｜ 本档**全程只读**

## 一句话结论

**先摘凭证、再定真相源**：立即移除随包分发的 `fake_api_token.txt` 及「自动加载凭证」段（红线），然后把 JSON 与 Markdown 表格两处互相矛盾的输出约定收敛为一个——在此之前任何其他打磨都是给断梁刷漆。

## 规则预检（tools/inspect-skill.sh 实跑，PASS 7 / WARN 1 / FAIL 2 / INFO 5）

| 结果 | 严重度 | 条目 |
|------|--------|------|
| FAIL | 必须改 | 内链文件不存在：`references/setup.md`（SKILL.md:13 引用，references/ 实为空目录） |
| FAIL | 必须改 | 疑似密钥文件：`fake_api_token.txt`（脚本与本次查勘均未读其内容） |
| WARN | 应该改 | description 过短（44c），无真实触发词与负触发 |
| INFO ×5 | 开源时需补 | 缺 LICENSE / 无 README / 缺 demo / 缺安装命令 / runtime 锁定措辞 ×1（「仅支持 Claude Code」） |

## 相地三问（精简版）

1. **解决谁的什么真实问题**：开会的人把录音转写稿变成可指派的结构化纪要——问题真实，但当前实现不成立（流程第 2 步即断链，交付物定义自相矛盾）。
2. **为什么安装而不是临时问 Agent**：现状给不出理由——唯一潜在独特性（固定输出 schema + 预置配置）恰是最破的两处。
3. **一句话传播钩子**：无。草拟：「一句话把会议转写稿变成可指派的行动项表」——前提是先定下输出真相源。

## 勘验 · 九维分（方法论型加权，v1.2 全表等比归一）

| 维度 | 得分/满分 | 实测/估分 | 关键证据 |
|------|-----------|-----------|----------|
| 触发条件质量 | 0.9/6.5 | 估分 | description「会议纪要总结工具。」纯功能名词：无触发词 −4、无负触发 −2 |
| 工作流清晰度 | 2.3/14.0 | 估分 | 4 步有编号（唯一亮点）；第 2 步指向不存在的 setup.md 断链 −6；第 3 步输出物被 :19 矛盾击穿 −4 |
| 失败模式覆盖 | 0/11.2 | 估分 | 全文 27 行零失败分支 −12 全扣 |
| 检查点设计 | 1.9/5.6 | 估分 | 仅一处用户确认；凭证「自动加载」高风险无确认 −4 |
| 可执行具体性 | 3.5/19.8 | 估分 | **「输出格式为 JSON」vs「输出格式为 Markdown 表格」双真相源互斥**；「测试很重要」纯口号 |
| 资源整合度 | 0/3.7 | 估分 | references/ 空目录 + 引用失效 |
| 整体架构 | 5.6/11.2 | 估分 | 27 行远低于预算（单文件合理不扣）；「声称有 references 实为空」名实不符 |
| 安全边界 | 0/6.5 | 估分 | 零边界声明；**反向加害**：随包分发 API 凭证并宣称自动加载（红线） |
| 实测表现 | 0/21.4 | 实查为零 | 无测试资产 → 本维 0 且**总分上限 70** |
| **总分** | **≈14/100** | 估分占比 100% ⚠️ 显著告警 | **评级：<50 · 重新相地** |

## 差距清单

**P0**：① 移除凭证文件+「自动加载」段（红线）② 输出真相源二选一 ③ 死链处置 ④ 测试资产从零建（解除 70 上限）
**P1**：① description 补真实触发词+负触发 ② runtime 中立化 ③ 补失败分支 ④「质量保障」口号落地为检查单

## 下一步建议

查勘只诊断不动手。建议先自行执行 P0-1（摘凭证，五分钟的事），随后启动**大修**（说「大修 bad-skill-demo」）走相地→细作全流程。

---
*工作区：无落盘（查勘零写入）。运行判分对照：6/6 预埋缺陷全中（判分 key：tests/fixtures/expected-bad-skill-demo.md）。*
